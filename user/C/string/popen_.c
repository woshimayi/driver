#include <stdio.h>
#include <string.h>
#include <stdlib.h>


int g = 0;

int main(int argc, char const *argv[])
{
	//    FILE * fd;
	//    char tmp[16] = {0};
	//    int cnt=0;
	//    fd = popen("cat firewall | grep redirect | wc -l", "r");
	//    if (!fd)
	//    {
	//        pclose(fd);
	//        return 0;
	//    }
	//
	//    cnt = fread(tmp, sizeof(char), sizeof(tmp), fd);
	//    if (!cnt)
	//    {
	//        pclose(fd);
	//        return 0;
	//    }
	//
	//    printf("tmp = %s dd = %d\n", tmp, atoi(tmp));




	return 0;
}



